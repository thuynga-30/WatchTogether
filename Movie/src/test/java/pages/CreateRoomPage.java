package pages;

import base.BasePage;
import org.openqa.selenium.WebDriver;

public class CreateRoomPage extends BasePage {
    public CreateRoomPage(WebDriver driver) {
        super(driver);
    }
}
